language: generic
sudo: false
git:
  depth: 10

install:
  - pip install coverage

script:
  - uname -a
  - which -a vim
  - vim --cmd version --cmd quit
  - ./install.sh

after_success:
  - bash <(curl -s https://codecov.io/bash)
